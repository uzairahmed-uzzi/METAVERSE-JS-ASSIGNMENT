//1
var roundedNumber = Math.round(5.5); // This will round 5.5 to 6

//2

var roundNum = Math.ceil(origNum); // This will round up to the nearest integer

//3
var roundNum = Math.floor(origNum); // This will round down to the nearest integer


//4
var originalNumber = 7.8; // Replace with your actual number
var roundedNumber = Math.round(originalNumber); // This will round to the nearest integer


//5

var roundedValue = Math.round(0.5); // This will round 0.5 to 0
