//1.
var sameWords = "captain";
var slicedPart = sameWords.slice(1, 3); // This will slice "ap"


//2.
var myString = "example";
var lengthOfString = myString.length; // This will store the length, which is 7, in lengthOfString


//3.
var animal = "elephant";
var seg = animal.slice(1, 5); // This will slice "leph"


//4. 
var myString = "example";
var lengthOfString = myString.length; // This will store the length, which is 7, in lengthOfString


//5.
var myString = "example";
var lengthOfString = myString.length; // This will store the length, which is 7, in lengthOfString
var slicedString = myString.slice(1, -3); // This will slice "xam"


//6. The value of `indx` is 3 because "be" first appears at index 3 in the string.

//7. The value of `indx` is 16 because "be" last appears at index 16 in the string.

//8.
var indx = text.lastIndexOf("go");


//9.
if (str[indexNum] !== undefined) {
   // Segment exists
}


//10. The character at index 2 in the string "abcde" is "c".

//11.
var cha = text.charAt(9); // This will assign the 10th character "o" to cha


//12.
var x = str.charAt(str.length - 1); // This will assign the last character to x


//13.
var cha = input.charAt(4); // This will assign the 5th character to cha


//14.
if (myString.charAt(2) === 'X') {
   // It's the particular character you're testing for
}

//15. 
var myString = "example";
var myArray = [];

for (var i = 0; i < myString.length; i++) {
   myArray.push(myString.charAt(i));
}


var revisedReply = reply.replace("no", "yes");


//16
var newStr = str.replace("1", "one");


//17.
var y = x.replace(/a/g, "z");
